{
  
  "name": "app",
  
  "version": "1.0.0",
  
  "type": "module",
  
  "devDependencies": {
  
    
  "tsc": "^2.0.4",
  
    
  "vite": "^5.0.0"
  
  },
  
  "scripts": {
    "dev": "npx vite",
    "build": "npx tsc && npx vite build",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  
  "dependencies": {
  
    
  "server": "^1.0.0"
  
  }
}
